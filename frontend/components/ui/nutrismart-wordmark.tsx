export function NutriSmartWordmark({ className = "" }: { className?: string }) {
  return (
    <span className={`font-extrabold ${className}`}>
      <span className="text-primary">Nutri</span>
      <span className="text-secondary">Smart</span>
    </span>
  )
}
